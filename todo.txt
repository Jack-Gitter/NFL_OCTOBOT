start to transform all data from api responses to classes
scoring plays should be its own class --> should have a "isOctopus" method on it
keep track of individual player octo count
keep trakc of total nfl octo count
